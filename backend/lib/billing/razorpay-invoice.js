/**
 * Razorpay invoice / subscription billing (for when paid plans are live).
 *
 * TODO: Every rupee amount on invoices must show GST breakdown (18% GST) once billing is live.
 * Display base amount + GST separately on invoices and in dashboard as required for compliance.
 */

module.exports = {};
